{
  "name": "lightcode",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:server": "bun --watch --env-file=apps/server/.env run apps/server/src/index.ts",
    "dev:cli": "bun --watch run apps/cli/src/index.tsx",
    "build": "bun --filter '*' build"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/react": "^19.2.14",
    "cli": "workspace:*",
    "typescript": "^5.6.0"
  }
}
